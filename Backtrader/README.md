# Stock_Predictor

Stock Prediction

Python 3.13.7

Run:

Install python

use virtual enviornmonment:

´´´bash
python -m venv .venv

Activate it:

´´´bash
source .venv/bin/activate

install requirements
pip install -r requirements.txt

run download:

python download_assets.py

run simulation:

python main.py